---
phase: 999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking
plan: 01a
type: tdd
wave: 1
depends_on: []
files_modified:
  - carta/embed/lifecycle.py
  - carta/tests/test_lifecycle.py
autonomous: true
requirements:
  - HASH-01
  - HASH-02

must_haves:
  truths:
    - "SHA256 hash computed correctly for markdown (LF-normalized) and PDF (raw bytes)"
    - "mtime fast-path skips hash computation when mtime unchanged"
  artifacts:
    - path: "carta/embed/lifecycle.py"
      provides: "compute_file_hash, needs_rehash (stdlib only, no Qdrant imports)"
      exports:
        - compute_file_hash
        - needs_rehash
    - path: "carta/tests/test_lifecycle.py"
      provides: "Tests for HASH-01, HASH-02 — no mocks needed (pure stdlib)"
  key_links:
    - from: "carta/embed/lifecycle.py::compute_file_hash"
      to: "hashlib.sha256"
      via: "LF-normalized bytes for .md, raw bytes for .pdf"
    - from: "carta/embed/lifecycle.py::needs_rehash"
      to: "os.path.getmtime"
      via: "float comparison against sidecar['file_mtime']"
---

<objective>
Create `carta/embed/lifecycle.py` with the two pure-stdlib hash and mtime primitives:
`compute_file_hash` and `needs_rehash`. No Qdrant dependency — these functions can be
tested in isolation without mocks.

Purpose: Hash and mtime fast-path are the decision-making core of the embed pipeline.
They must be correct and well-tested before Qdrant lifecycle ops build on top of them.

Output: `carta/embed/lifecycle.py` (partial — HASH-01 and HASH-02 only), fully tested.
Plan 01b adds the remaining Qdrant-dependent functions to the same module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-CONTEXT.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-RESEARCH.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: compute_file_hash (HASH-01)</name>
  <files>carta/embed/lifecycle.py, carta/tests/test_lifecycle.py</files>
  <behavior>
    - compute_file_hash(path: Path) -> str
    - Markdown (.md): read_bytes(), replace(b'\r\n', b'\n'), sha256.hexdigest()
    - PDF (.pdf): read_bytes() raw, sha256.hexdigest() (no normalization)
    - Other text files: treat as markdown (LF normalization)
    - Returns lowercase hex string
    - Test: md file with CRLF and LF bytes produce same hash
    - Test: pdf file bytes are not normalized (verify raw bytes used)
    - Test: unknown extension treated as markdown
  </behavior>
  <implementation>
    After tests pass (RED -> GREEN):
    - Module: carta/embed/lifecycle.py
    - Import only: hashlib, os, os.path, datetime, pathlib.Path
    - No imports from carta.embed.embed (lifecycle.py is a leaf module)
    - 4-space indent, PEP 8, docstring on public function
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_lifecycle.py::test_compute_file_hash -v</automated>
  </verify>
  <done>compute_file_hash returns identical hash for CRLF and LF markdown; PDF hashed raw; tests pass</done>
</task>

<task type="tdd">
  <name>Task 2: needs_rehash mtime fast-path (HASH-02)</name>
  <files>carta/embed/lifecycle.py, carta/tests/test_lifecycle.py</files>
  <behavior>
    - needs_rehash(file_path: Path, sidecar: dict) -> bool
    - Returns True if sidecar has no "file_mtime" key
    - Returns False if os.path.getmtime(file_path) == sidecar["file_mtime"] (float)
    - Returns True if mtime values differ
    - Test: sidecar with no file_mtime key -> True
    - Test: sidecar file_mtime matches actual mtime -> False
    - Test: sidecar file_mtime differs from actual mtime -> True
  </behavior>
  <implementation>
    Add needs_rehash below compute_file_hash in lifecycle.py.
    Use os.path.getmtime — no mock needed in tests (use a real temp file via tmp_path fixture).
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_lifecycle.py::test_needs_rehash -v</automated>
  </verify>
  <done>needs_rehash returns correct bool for all three cases; all tests pass</done>
</task>

</tasks>

<verification>
pytest carta/tests/test_lifecycle.py -v -k "hash or rehash"
No imports from qdrant_client in lifecycle.py at end of this plan.
</verification>

<success_criteria>
- pytest carta/tests/test_lifecycle.py -q exits 0
- compute_file_hash and needs_rehash exported from carta.embed.lifecycle
- No qdrant_client import in lifecycle.py (added in Plan 01b)
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-01a-SUMMARY.md`
</output>

---
---
phase: 999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking
plan: 01b
type: tdd
wave: 1
depends_on:
  - 999.1-01a
files_modified:
  - carta/embed/lifecycle.py
  - carta/tests/test_lifecycle.py
autonomous: true
requirements:
  - LIFECYCLE-01
  - LIFECYCLE-02
  - LIFECYCLE-03
  - STALE-01

must_haves:
  truths:
    - "Qdrant set_payload called with Filter to bulk-mark chunks stale"
    - "Expired orphan chunks (orphaned_at < now - ttl) are scrolled and deleted from Qdrant"
    - "Protected doc_types (quirk, bug-note, helpful-note) are never touched by orphan cleanup"
    - "Stale alert message printed to stdout when threshold percentage exceeded"
    - "Pre-999.1 chunks missing sidecar_id are treated as generation-0 and ignored by lifecycle ops"
  artifacts:
    - path: "carta/embed/lifecycle.py"
      provides: "mark_sidecar_stale, cleanup_expired_orphans, is_protected_doc_type, check_stale_alert"
      exports:
        - mark_sidecar_stale
        - cleanup_expired_orphans
        - is_protected_doc_type
        - check_stale_alert
    - path: "carta/tests/test_lifecycle.py"
      provides: "Tests for LIFECYCLE-01, LIFECYCLE-02, LIFECYCLE-03, STALE-01 — mocks qdrant_client"
  key_links:
    - from: "carta/embed/lifecycle.py::mark_sidecar_stale"
      to: "qdrant_client.set_payload"
      via: "Filter(must=[FieldCondition(key='sidecar_id', ...)])"
    - from: "carta/embed/lifecycle.py::cleanup_expired_orphans"
      to: "qdrant_client.scroll + qdrant_client.delete"
      via: "DatetimeRange(lt=cutoff) filter, guards against missing sidecar_id"
---

<objective>
Extend `carta/embed/lifecycle.py` (created in Plan 01a) with the four Qdrant-dependent
lifecycle functions: `mark_sidecar_stale`, `cleanup_expired_orphans`, `is_protected_doc_type`,
and `check_stale_alert`.

Migration boundary: Existing Qdrant chunks embedded before Phase 999.1 have no `sidecar_id`
in their payload. These are treated as generation-0 chunks. `mark_sidecar_stale` and
`cleanup_expired_orphans` MUST guard against absent `sidecar_id` — they are no-ops for
such chunks until the chunk is re-embedded with the new payload schema (Plan 02).

Purpose: All Qdrant lifecycle ops centralized in one module so pipeline.py and server.py
call clean functions without duplicating filter patterns.

Output: Completed `carta/embed/lifecycle.py` with all six public functions, fully tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-CONTEXT.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-RESEARCH.md

<interfaces>
Qdrant imports confirmed available (qdrant-client 1.17.1):
```python
from qdrant_client.models import (
    Filter, FieldCondition, MatchValue, DatetimeRange,
    IsNullCondition, PayloadField,
)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: mark_sidecar_stale with pre-999.1 guard (LIFECYCLE-01)</name>
  <files>carta/embed/lifecycle.py, carta/tests/test_lifecycle.py</files>
  <behavior>
    - mark_sidecar_stale(client, collection_name: str, sidecar_id: str, now: datetime) -> None
    - Calls client.set_payload with payload={stale_as_of: now.isoformat(), status: "stale"}
    - Filter: FieldCondition(key="sidecar_id", match=MatchValue(value=sidecar_id))
    - GUARD: Does NOT call set_payload if sidecar_id is empty string or None
      (pre-999.1 migration boundary — chunks without sidecar_id are generation-0, ignored)
    - Test: valid sidecar_id -> set_payload called once with correct filter
    - Test: sidecar_id="" -> set_payload NOT called (silent no-op)
    - Test: sidecar_id=None -> set_payload NOT called (silent no-op)
  </behavior>
  <implementation>
    Add to lifecycle.py after needs_rehash. Use unittest.mock.MagicMock for qdrant client in tests.
    The empty-string guard documents the migration boundary: pre-999.1 chunks missing sidecar_id
    are inert to lifecycle ops until re-embedded by Plan 02 schema.
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_lifecycle.py::test_mark_sidecar_stale -v</automated>
  </verify>
  <done>mark_sidecar_stale calls set_payload for valid IDs; no-ops for empty/None sidecar_id; tests pass</done>
</task>

<task type="tdd">
  <name>Task 2: cleanup_expired_orphans with pre-999.1 guard (LIFECYCLE-02)</name>
  <files>carta/embed/lifecycle.py, carta/tests/test_lifecycle.py</files>
  <behavior>
    - cleanup_expired_orphans(client, collection_name: str, ttl_days: int, now: datetime) -> int
    - Builds cutoff = now - timedelta(days=ttl_days)
    - Scrolls with DatetimeRange(lt=cutoff) on orphaned_at field
    - GUARD: After scroll, filter out any points where payload["sidecar_id"] is absent or ""
      (pre-999.1 chunks — treat as generation-0, do not delete until re-embedded)
    - Deletes only matched point IDs that pass the sidecar_id guard
    - Returns count of deleted points
    - Returns 0 if scroll returns empty list (no error)
    - Protected collections: caller responsibility (lifecycle.py does not filter by doc_type)
    - Test: scroll returns 3 points, 1 has no sidecar_id -> 2 deleted, returns 2
    - Test: empty scroll -> returns 0, delete NOT called
    - Test: all points missing sidecar_id -> returns 0, delete NOT called
  </behavior>
  <implementation>
    Add to lifecycle.py. Tests use MagicMock for client.scroll and client.delete.
    The sidecar_id guard is explicit: for p in scroll_result if p.payload.get("sidecar_id").
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_lifecycle.py::test_cleanup_expired_orphans -v</automated>
  </verify>
  <done>cleanup_expired_orphans skips pre-999.1 chunks (no sidecar_id); deletes only eligible points; returns correct count</done>
</task>

<task type="tdd">
  <name>Task 3: is_protected_doc_type and check_stale_alert (LIFECYCLE-03, STALE-01)</name>
  <files>carta/embed/lifecycle.py, carta/tests/test_lifecycle.py</files>
  <behavior>
    - is_protected_doc_type(doc_type: str) -> bool
      - Returns True for: "quirk", "bug-note", "helpful-note"
      - Returns False for all others ("doc", "datasheet", "manual", "session", unknown strings)
      - Test: each protected type returns True; three non-protected types return False

    - check_stale_alert(stale_count: int, total_count: int, threshold: float) -> str | None
      - Returns None if total_count == 0
      - Returns None if stale_count / total_count < threshold
      - Returns warning string if stale_count / total_count >= threshold
      - Warning string includes the percentage and "carta embed --force-stale" hint
      - threshold is a fraction (0.30 = 30%)
      - Test: 0/0 -> None; 0/10 -> None; 3/10 (threshold=0.30) -> None; 3/10 (threshold=0.29) -> str
      - Test: returned string contains "carta embed --force-stale"
  </behavior>
  <implementation>
    Add to lifecycle.py. No mocks needed — pure logic functions.
    is_protected_doc_type: use a frozenset constant PROTECTED_DOC_TYPES = frozenset({"quirk", "bug-note", "helpful-note"}).
    check_stale_alert: use integer percent in warning string (e.g., "30% of embedded docs are stale").
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_lifecycle.py::test_is_protected_doc_type carta/tests/test_lifecycle.py::test_check_stale_alert -v</automated>
  </verify>
  <done>is_protected_doc_type returns correct bool for all types; check_stale_alert returns None or warning string; all tests pass</done>
</task>

</tasks>

<verification>
pytest carta/tests/test_lifecycle.py -v
All six public functions exported from carta.embed.lifecycle.
No circular imports (lifecycle.py does not import from carta.embed.embed).
</verification>

<success_criteria>
- pytest carta/tests/test_lifecycle.py -q exits 0
- All six functions exported: compute_file_hash, needs_rehash, mark_sidecar_stale,
  cleanup_expired_orphans, is_protected_doc_type, check_stale_alert
- Pre-999.1 migration guard present in mark_sidecar_stale and cleanup_expired_orphans
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-01b-SUMMARY.md`
</output>

---
---
phase: 999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - carta/embed/induct.py
  - carta/embed/embed.py
  - carta/config.py
  - carta/tests/test_induct.py
  - carta/tests/test_embed.py
autonomous: true
requirements:
  - SIDECAR-01
  - PAYLOAD-01

must_haves:
  truths:
    - "generate_sidecar_stub returns dict with file_hash, hash_algorithm, generation, last_hash_check_at, version_history fields"
    - "upsert_chunks includes doc_generation, stale_as_of, superseded_at, orphaned_at, sidecar_id, chunk_source_hash in every PointStruct payload"
    - "upsert_chunks uses _point_id_versioned when chunk contains doc_generation, falls back to _point_id for chunks without it"
    - "collection_for_doc_type maps protected types to {project}_notes and all others to {project}_doc"
    - "_point_id_versioned produces different UUID than _point_id for same slug/chunk_index when generation differs"
  artifacts:
    - path: "carta/embed/induct.py"
      provides: "Updated generate_sidecar_stub with lifecycle fields"
    - path: "carta/embed/embed.py"
      provides: "Updated upsert_chunks payload schema + _point_id_versioned helper"
    - path: "carta/config.py"
      provides: "collection_for_doc_type helper"
    - path: "carta/tests/test_induct.py"
      provides: "Tests for SIDECAR-01"
    - path: "carta/tests/test_embed.py"
      provides: "Tests for PAYLOAD-01"
  key_links:
    - from: "carta/embed/embed.py::upsert_chunks"
      to: "_point_id_versioned"
      via: "chunk.get('doc_generation') present -> use versioned ID; absent -> fall back to _point_id"
    - from: "carta/config.py::collection_for_doc_type"
      to: "carta/embed/embed.py::upsert_chunks"
      via: "caller passes collection_name derived from doc_type"
---

<objective>
Extend the sidecar stub schema and Qdrant chunk payload schema with lifecycle fields.
Add `_point_id_versioned` to `embed.py` and `collection_for_doc_type` to `config.py`.

Purpose: Every embedded chunk and every sidecar file carries the metadata needed for
generation-aware lifecycle management. Without these fields, `lifecycle.py` functions
cannot mark stale, detect orphans, or clean up expired chunks.

Output: Updated `induct.py`, `embed.py`, `config.py` with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@carta/embed/induct.py
@carta/embed/embed.py
@carta/config.py
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-CONTEXT.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-RESEARCH.md

<interfaces>
From carta/embed/embed.py (existing _point_id pattern to extend):
```python
import hashlib, uuid

def _point_id(slug: str, chunk_index: int) -> str:
    raw = f"{slug}:{chunk_index}"
    return str(uuid.UUID(hashlib.md5(raw.encode()).hexdigest()))
```

From carta/config.py (collection_name pattern):
```python
def collection_name(cfg: dict, type_: str) -> str:
    project = cfg.get("project", {}).get("name", "unnamed")
    return f"{project}_{type_}"
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: generate_sidecar_stub lifecycle fields (SIDECAR-01)</name>
  <files>carta/embed/induct.py, carta/tests/test_induct.py</files>
  <behavior>
    - generate_sidecar_stub(slug, doc_type, file_type, current_path, ...) returns dict containing:
      - file_hash: null (populated on first embed)
      - hash_algorithm: "sha256"
      - generation: 0
      - last_hash_check_at: null
      - version_history: []
      - status: "pending" (unchanged from existing)
      - All existing fields preserved unchanged
    - Test: returned dict contains all six new keys with correct initial values
    - Test: existing fields (slug, doc_type, file_type, current_path, status) still present
    - Test: version_history is a list (not None, not missing)
  </behavior>
  <implementation>
    After tests pass: add new fields to the stub dict in generate_sidecar_stub. Do NOT change existing fields or their values.
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_induct.py -v -k "sidecar_stub"</automated>
  </verify>
  <done>generate_sidecar_stub returns all new lifecycle fields at correct initial values; existing fields unchanged; tests pass</done>
</task>

<task type="tdd">
  <name>Task 2: upsert_chunks payload + _point_id_versioned (PAYLOAD-01)</name>
  <files>carta/embed/embed.py, carta/tests/test_embed.py</files>
  <behavior>
    - _point_id_versioned(slug, chunk_index, generation) in embed.py:
      - md5(f"{slug}:{chunk_index}:g{generation}") -> UUID string
      - _point_id("x", 0) != _point_id_versioned("x", 0, 1) (different format strings -> different IDs)
      - _point_id_versioned("x", 0, 1) != _point_id_versioned("x", 0, 2)

    - upsert_chunks payload routing (BLOCKER-3 fix):
      - If chunk dict contains "doc_generation" key: calls _point_id_versioned(slug, chunk_index, doc_generation)
      - If chunk dict does NOT contain "doc_generation": falls back to _point_id(slug, chunk_index) (backward compat)

    - upsert_chunks each PointStruct payload includes new fields:
      - doc_generation: int (from chunk dict, default 1 if absent)
      - stale_as_of: null (initial value)
      - superseded_at: null (initial value)
      - orphaned_at: null (initial value)
      - sidecar_id: str (from chunk dict, default "" if absent)
      - chunk_source_hash: str (from chunk dict, default "" if absent)
      - All existing payload fields preserved unchanged

    - collection_for_doc_type(cfg, doc_type) in config.py:
      - "quirk" -> collection_name(cfg, "notes")
      - "bug-note" -> collection_name(cfg, "notes")
      - "helpful-note" -> collection_name(cfg, "notes")
      - "doc" -> collection_name(cfg, "doc")
      - "datasheet" -> collection_name(cfg, "doc")
      - "manual" -> collection_name(cfg, "doc")
      - "session" -> collection_name(cfg, "session")
      - unknown string -> collection_name(cfg, "doc") (safe default)

    - Test: _point_id_versioned("x", 0, 1) != _point_id("x", 0)
    - Test: chunk with doc_generation=2 uses _point_id_versioned; chunk without uses _point_id
    - Test: PointStruct payload contains all six new keys
    - Test: collection_for_doc_type maps each protected type to _notes collection
  </behavior>
  <implementation>
    After tests pass:
    - embed.py: Add _point_id_versioned as private helper below existing _point_id.
      Update upsert_chunks to use versioned ID when doc_generation present in chunk.
      Use chunk.get("doc_generation") is not None as the branch condition.
      DO NOT remove or rename _point_id — used by existing embedded collections.
    - config.py: Add collection_for_doc_type(cfg, doc_type) -> str after existing collection_name().
    - Test files use unittest.mock to mock qdrant_client — do not require live Qdrant.
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_embed.py -v -k "versioned or payload or collection_for"</automated>
  </verify>
  <done>_point_id_versioned produces distinct IDs per generation; upsert_chunks routes to versioned ID when doc_generation present; all new payload fields present; tests pass</done>
</task>

</tasks>

<verification>
pytest carta/tests/test_induct.py carta/tests/test_embed.py -q
Existing _point_id still present and unchanged in embed.py.
collection_for_doc_type exported from carta.config.
</verification>

<success_criteria>
- pytest carta/tests/test_induct.py carta/tests/test_embed.py -q exits 0
- _point_id_versioned present in embed.py; _point_id unchanged
- upsert_chunks uses versioned ID for chunks with doc_generation, _point_id for legacy chunks
- collection_for_doc_type exported from config.py
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-02-SUMMARY.md`
</output>

---
---
phase: 999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking
plan: 03
type: execute
wave: 2
depends_on:
  - 999.1-01b
  - 999.1-02
files_modified:
  - carta/embed/pipeline.py
autonomous: true
requirements:
  - HASH-01
  - HASH-02
  # Note: STALE-01 is implemented in Plan 01b (check_stale_alert). Plan 03 consumes
  # check_stale_alert from lifecycle.py — it does not re-implement it.

must_haves:
  truths:
    - "run_embed_file skips hash computation when file mtime is unchanged (calls needs_rehash first)"
    - "run_embed_file computes SHA256 hash when mtime changes; marks file stale if hash differs"
    - "Sidecar generation is incremented when file content changes (hash mismatch)"
    - "Stale alert printed to stdout at end of embed run when threshold exceeded"
    - "version_history in sidecar trimmed to max_generations entries on each update"
    - "Pre-999.1 chunks (no sidecar_id) are not passed to mark_sidecar_stale — pipeline guards this boundary"
  artifacts:
    - path: "carta/embed/pipeline.py"
      provides: "mtime fast-path + hash comparison in run_embed_file; stale alert at end of run_embed"
  key_links:
    - from: "carta/embed/pipeline.py::run_embed_file"
      to: "carta.embed.lifecycle.needs_rehash"
      via: "import and call before hash computation"
    - from: "carta/embed/pipeline.py::run_embed_file"
      to: "carta.embed.lifecycle.compute_file_hash"
      via: "called when needs_rehash returns True"
    - from: "carta/embed/pipeline.py::run_embed"
      to: "carta.embed.lifecycle.check_stale_alert"
      via: "called after embed loop with stale/total counts; result printed to stdout if not None"
---

<objective>
Wire the lifecycle functions from Plans 01a/01b into `pipeline.py`. Add the mtime fast-path
and hash comparison to `run_embed_file`, increment generation on hash mismatch, trim
`version_history`, and emit the stale alert after the embed run.

Migration boundary: pipeline.py must guard against passing an empty sidecar_id to
`mark_sidecar_stale`. Check `sidecar.get("sidecar_id")` before calling — pre-999.1 sidecars
that predate the Plan 02 schema do not have this field. The lifecycle.py guard (Plan 01b)
provides defense-in-depth, but pipeline.py should not call it with empty IDs.

Purpose: Without this wiring, `lifecycle.py` and the schema changes in Plan 02 are
inert — `pipeline.py` is the orchestrator that drives the per-file lifecycle decisions.

Output: Updated `carta/embed/pipeline.py` with hash-aware change detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@carta/embed/pipeline.py
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-CONTEXT.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mtime fast-path + hash comparison in run_embed_file</name>
  <files>carta/embed/pipeline.py</files>
  <behavior>
    - Test: file with unchanged mtime -> needs_rehash returns False -> file skipped (no hash call)
    - Test: mtime changed, hash unchanged -> mtime updated in sidecar, status stays "embedded", no stale mark
    - Test: mtime changed, hash changed -> sidecar generation incremented, status set "stale", stale_as_of set, version_history entry appended
    - Test: version_history trimmed to max_generations (default 2) after append
    - Test: sidecar with no sidecar_id -> mark_sidecar_stale NOT called (migration boundary guard)
  </behavior>
  <action>
    In run_embed_file (pipeline.py):

    1. After reading sidecar, call `needs_rehash(file_path, sidecar)` from `carta.embed.lifecycle`.
       If False: update sidecar file_mtime only (no hash compute), return early with status "skipped".

    2. If True: call `compute_file_hash(file_path)` from `carta.embed.lifecycle`.
       Compare with sidecar.get("file_hash").
       - Hash unchanged: write updated file_mtime + last_hash_check_at to sidecar. Return "skipped".
       - Hash changed: increment sidecar["generation"] (default 0 -> 1), set sidecar["status"] = "stale",
         set sidecar["stale_as_of"] = now ISO string, set sidecar["file_hash"] = new_hash,
         set sidecar["last_hash_check_at"] = now ISO string.
         Append {"hash": new_hash, "generation": new_gen, "indexed_at": now} to sidecar["version_history"].
         Trim version_history to last cfg.get("embed", {}).get("max_generations", 2) entries.
         Proceed with re-embed (do not skip).

    3. Before calling mark_sidecar_stale, guard: if not sidecar.get("sidecar_id"): skip the call.
       (Pre-999.1 migration boundary — sidecar_id absent on sidecars predating Plan 02 schema.)

    Config keys: `embed.stale_alert_threshold` (default 0.30), `embed.max_generations` (default 2).
    Use `from carta.embed.lifecycle import needs_rehash, compute_file_hash`.
  </action>
  <verify>
    <automated>pytest carta/tests/ -x -q -k "pipeline or fastpath or hash"</automated>
  </verify>
  <done>mtime fast-path skips unchanged files; hash mismatch sets stale and increments generation; version_history trimmed; sidecar_id guard present</done>
</task>

<task type="auto">
  <name>Task 2: Stale alert after embed run</name>
  <files>carta/embed/pipeline.py</files>
  <action>
    In run_embed (or the function that invokes the per-file embed loop and collects results):

    After the embed loop completes, count:
    - total_embedded: all sidecars with status in ("embedded", "stale")
    - stale_count: sidecars with status == "stale"

    Call `check_stale_alert(stale_count, total_embedded, threshold)` from `carta.embed.lifecycle`
    where threshold = cfg.get("embed", {}).get("stale_alert_threshold", 0.30).

    If the result is not None, print it to stdout (plain print — not stderr).

    Per CLAUDE.md: stdout is reserved for JSON-RPC in MCP server context. This alert fires
    from the CLI embed command path, not from MCP server.py — no conflict.
  </action>
  <verify>
    <automated>pytest carta/tests/ -x -q -k "stale_alert or embed_run"</automated>
  </verify>
  <done>Stale alert message printed to stdout after embed loop when stale fraction exceeds configured threshold</done>
</task>

</tasks>

<verification>
pytest carta/tests/ -q
All existing tests pass. New tests for pipeline mtime fast-path and stale alert pass.
No regressions in test_cli.py, test_config.py, test_bootstrap.py, test_version.py.
</verification>

<success_criteria>
- pytest carta/tests/ -q exits 0 with no failures
- needs_rehash skips hash when mtime unchanged (confirmed by test mock counting calls to compute_file_hash)
- Hash mismatch increments generation and sets status="stale" in sidecar
- Stale alert fires when stale/total >= threshold
- sidecar_id guard present before mark_sidecar_stale calls
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-03-SUMMARY.md`
</output>

---
---
phase: 999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking
plan: 04
type: tdd
wave: 2
depends_on:
  - 999.1-01b
  - 999.1-02
files_modified:
  - carta/mcp/server.py
  - carta/embed/pipeline.py
  - carta/tests/test_mcp_server.py
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03

must_haves:
  truths:
    - "carta_embed(scope='file', path='docs/x.md') calls run_embed_file for that specific file"
    - "carta_embed(scope='stale') discovers stale sidecars and re-embeds them"
    - "carta_embed(scope='file') without path returns error dict, does not crash"
    - "carta_embed(scope='all') preserves existing full-collection behavior"
    - "MCP tool JSON schema has scope as enum ['stale', 'file', 'all'] with default 'all'"
  artifacts:
    - path: "carta/mcp/server.py"
      provides: "Extended carta_embed with scope + path parameters"
    - path: "carta/embed/pipeline.py"
      provides: "discover_stale_files helper (added in this plan)"
    - path: "carta/tests/test_mcp_server.py"
      provides: "Tests for MCP-01, MCP-02, MCP-03"
  key_links:
    - from: "carta/mcp/server.py::carta_embed"
      to: "carta/embed/pipeline.py::run_embed_file"
      via: "scope='file' path"
    - from: "carta/mcp/server.py::carta_embed"
      to: "discover_stale_files (new helper in pipeline.py)"
      via: "scope='stale'"
---

<objective>
Extend the `carta_embed` MCP tool in `server.py` with `scope: Literal['stale', 'file', 'all']`
and optional `path` parameter. Add `discover_stale_files` helper to `pipeline.py` for
scope="stale" discovery.

Purpose: Claude Code can autonomously trigger targeted re-embedding mid-session without
human intervention — per D-11 (Agent Autonomy via MCP, CONTEXT.md §11).

Output: Updated `carta/mcp/server.py` and `carta/embed/pipeline.py` with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@carta/mcp/server.py
@carta/embed/pipeline.py
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-CONTEXT.md
@.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-RESEARCH.md

<interfaces>
From research — confirmed FastMCP 1.26.0 signature:
```python
from typing import Literal, Optional

@mcp_server.tool()
def carta_embed(
    scope: Literal["stale", "file", "all"] = "all",
    path: Optional[str] = None,
    force: bool = False,
) -> dict:
    ...
```

Generated JSON Schema (confirmed):
```json
{
  "scope": {"type": "string", "enum": ["stale", "file", "all"], "default": "all"},
  "path":  {"anyOf": [{"type": "string"}, {"type": "null"}], "default": null}
}
```

Pitfall: Old callers may pass path as first positional arg (old signature: carta_embed(path, force)).
Guard: if scope not in ("stale", "file", "all") and path is None: treat scope value as path, set scope="file".
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: discover_stale_files in pipeline.py</name>
  <files>carta/embed/pipeline.py, carta/tests/test_mcp_server.py</files>
  <behavior>
    - discover_stale_files(repo_root: Path) -> list[Path]
    - Reads all .embed-meta.yaml sidecars under repo_root via rglob
    - Returns file paths (the document path, not the sidecar path) where sidecar status == "stale"
    - Analogous to existing discover_pending_files but filters for status "stale"
    - Test: two sidecars, one stale, one embedded -> returns one Path
    - Test: no stale sidecars -> returns empty list
    - Test: sidecar missing status key -> not included in results
  </behavior>
  <implementation>
    Add discover_stale_files as a public function in pipeline.py alongside discover_pending_files.
    Follow same rglob pattern as discover_pending_files; filter by sidecar["status"] == "stale".
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_mcp_server.py -v -k "discover_stale"</automated>
  </verify>
  <done>discover_stale_files returns correct stale file paths; empty list when none stale; tests pass</done>
</task>

<task type="tdd">
  <name>Task 2: carta_embed scope parameter (MCP-01, MCP-02, MCP-03)</name>
  <files>carta/mcp/server.py, carta/tests/test_mcp_server.py</files>
  <behavior>
    - MCP-01: carta_embed(scope="file", path="docs/chip.pdf")
      - Resolves path relative to repo_root if not absolute
      - Calls run_embed_file(file_path, cfg, client)
      - Returns {"status": "ok", "file": path, "scope": "file"}

    - MCP-02: carta_embed(scope="stale")
      - Calls discover_stale_files(repo_root) -> list of Path objects
      - For each stale file, calls run_embed_file
      - Returns {"status": "ok", "scope": "stale", "reembedded": N}

    - MCP-03: carta_embed(scope="file") with path=None
      - Returns {"error": "invalid_request", "detail": "path is required when scope='file'"}
      - Does NOT raise an exception

    - MCP-04 (backward compat): carta_embed(scope="all")
      - Preserves existing full-collection behavior
      - Returns existing response format

    - Backward compat guard:
      - if scope not in ("stale", "file", "all") and path is None: treat scope as path, set scope="file"

    - Test each of the four cases with mocked pipeline functions
    - Test backward compat guard with old positional-arg call pattern
  </behavior>
  <implementation>
    After tests pass:
    - server.py: Replace existing carta_embed signature with new signature.
      lifecycle warnings go to stderr via print(..., file=sys.stderr) — stdout is JSON-RPC.
    - test_mcp_server.py: Mock run_embed_file and discover_stale_files with unittest.mock.patch.
  </implementation>
  <verify>
    <automated>pytest carta/tests/test_mcp_server.py -v -k "carta_embed"</automated>
  </verify>
  <done>All four scope cases return correct response; backward compat guard works; no exceptions on invalid scope; tests pass</done>
</task>

</tasks>

<verification>
pytest carta/tests/test_mcp_server.py -v
carta/embed/pipeline.py contains discover_stale_files (public function).
carta/mcp/server.py carta_embed signature uses Literal["stale", "file", "all"].
</verification>

<success_criteria>
- pytest carta/tests/test_mcp_server.py -q exits 0
- discover_stale_files exported from carta.embed.pipeline
- carta_embed handles all four scope values without raising
- Backward compat guard present for old positional-arg callers
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-sidecar-enrichment-agent-populated-notes-sidecars-qdrant-chunks-cross-linking/999.1-04-SUMMARY.md`
</output>
