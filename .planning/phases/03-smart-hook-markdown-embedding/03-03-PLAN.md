---
phase: 03-smart-hook-markdown-embedding
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: false
requirements: [HOOK-01, HOOK-02, HOOK-03, HOOK-04, HOOK-05, HOOK-06, HOOK-07, EMBED-01]
must_haves:
  truths:
    - "Hook injects relevant documentation into Claude session on high-score prompt"
    - "Hook stays silent on irrelevant prompt (no context noise)"
    - "Markdown files can be embedded via carta embed"
    - "All automated tests pass as regression gate"
---

<objective>
Verify the complete Phase 3 deliverable — smart hook context injection and markdown embedding — with a human smoke test in a live Claude Code session.

Purpose: Automated tests cover unit behavior; this checkpoint confirms end-to-end behavior in the real Claude Code hook environment.
Output: Human-verified confirmation that hook injection works in practice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-smart-hook-markdown-embedding/03-CONTEXT.md
@.planning/phases/03-smart-hook-markdown-embedding/03-01-SUMMARY.md
@.planning/phases/03-smart-hook-markdown-embedding/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks and full regression suite</name>
  <files></files>
  <read_first>
    - .planning/phases/03-smart-hook-markdown-embedding/03-01-SUMMARY.md
    - .planning/phases/03-smart-hook-markdown-embedding/03-02-SUMMARY.md
  </read_first>
  <action>
    Run the full automated verification sequence:

    1. **Full test suite:** `python -m pytest carta/ -x -v`
    2. **Config check:** Verify proactive_recall defaults are correct:
       `python -c "from carta.config import DEFAULTS; pr = DEFAULTS['proactive_recall']; assert pr['high_threshold'] == 0.85; assert pr['low_threshold'] == 0.60; assert pr['max_results'] == 5; assert pr['judge_timeout_s'] == 3; print('Config OK')"`
    3. **Entry point check:** `which carta-hook` (should resolve after pip install -e .)
       If not found, run `pip install -e .` first.
    4. **Module import check:** `python -c "from carta.hook.hook import main; print('Hook module OK')"`
    5. **Markdown extractor check:** `python -c "from carta.embed.parse import extract_markdown_text; print('Markdown extractor OK')"`
    6. **Supported extensions check:** `python -c "from carta.embed.pipeline import _SUPPORTED_EXTENSIONS; assert '.md' in _SUPPORTED_EXTENSIONS; print('Extensions OK')"`

    Report results of all checks. If any fail, diagnose and fix before proceeding to checkpoint.
  </action>
  <verify>
    <automated>python -m pytest carta/ -x -q && python -c "from carta.hook.hook import main; from carta.embed.parse import extract_markdown_text; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest carta/ -x -q` exits 0
    - `which carta-hook` outputs a path
    - All 6 checks above print OK / pass
  </acceptance_criteria>
  <done>All automated checks pass. System ready for manual smoke test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test hook in live Claude Code session</name>
  <files></files>
  <action>
    Present the smoke test instructions to the user and wait for their verification.
  </action>
  <what-built>
    Smart hook that automatically injects relevant documentation context when you submit a prompt in Claude Code. Three-zone score routing: high-confidence matches inject immediately, low-confidence discarded, gray zone uses Ollama judge. Markdown files can now be embedded alongside PDFs.
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Ensure Qdrant is running: `docker ps | grep qdrant` (start with `docker run -d -p 6333:6333 qdrant/qdrant` if needed)
    2. Ensure Ollama is running: `ollama list` (start with `ollama serve &` if needed)
    3. Pull judge model if not present: `ollama pull qwen2.5:0.5b`
    4. Ensure some documents are embedded: `carta embed` (run from project root)

    **Test 1 — High-score injection:**
    1. Open a new Claude Code session in this project
    2. Submit a prompt closely matching an embedded document (e.g., reference a specific API or concept from your docs)
    3. Expected: You should see injected context appearing before Claude's response, with `## Relevant documentation` header and source attributions

    **Test 2 — Noise gate (no injection):**
    1. Submit a completely unrelated prompt (e.g., "What is the weather today?")
    2. Expected: No injected documentation — prompt proceeds normally

    **Test 3 — Markdown embedding:**
    1. Place a test `.md` file in your docs directory
    2. Run `carta embed`
    3. Run `carta search "topic from the markdown file"`
    4. Expected: Search returns results from the markdown file

    **If hook stdout format is wrong** (Claude shows an error or garbled text instead of clean injection):
    - Check stderr: run `echo '{"prompt":"test"}' | carta-hook 2>/tmp/hook-stderr.log; cat /tmp/hook-stderr.log`
    - The stdout format may need adjustment — report what Claude Code displayed
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — human verification required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms hook injects context on a high-score prompt
    - User confirms no injection on an irrelevant prompt
    - User confirms `carta embed` processes a .md file and `carta search` returns markdown results
  </acceptance_criteria>
  <done>User has verified hook injection works in live Claude Code session.</done>
  <resume-signal>Type "approved" if hook injection works, or describe what went wrong (include any error output from stderr)</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green (Task 1)
- Human confirms hook injection works in live Claude Code session (Task 2)
</verification>

<success_criteria>
1. All automated tests pass
2. Hook injects relevant documentation on high-confidence prompt
3. Hook stays silent on irrelevant prompt
4. Markdown files are embeddable and searchable
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-hook-markdown-embedding/03-03-SUMMARY.md`
</output>
