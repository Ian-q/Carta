---
phase: 01-pipeline-reliability-mcp-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: []
autonomous: false
requirements: [MCP-01, PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05, MCP-06, MCP-07]

must_haves:
  truths:
    - "Full test suite passes with zero failures"
    - "carta-mcp process starts and produces valid JSON-RPC initialization on stdout"
    - "No stdout pollution from carta-mcp — only JSON-RPC framing on stdout, logs on stderr"
    - ".mcp.json is the sole Carta registration; no plugin cache entry exists"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite across both plans, verify carta-mcp starts cleanly, and confirm no plugin cache residue exists. Human verifies the MCP server produces a clean JSON-RPC stream.

Purpose: Gate check before Phase 2 depends on this foundation. Catches any cross-plan integration issues.
Output: Verification results; phase ready for Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-reliability-mcp-foundation/01-01-SUMMARY.md
@.planning/phases/01-pipeline-reliability-mcp-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full integration test suite</name>
  <files></files>
  <read_first>carta/embed/tests/test_embed.py, carta/mcp/tests/test_server.py, carta/install/tests/test_bootstrap.py</read_first>
  <action>
Run the full test suite to verify no cross-plan regressions:

```bash
python3 -m pytest -x -v 2>&1
```

If any test fails, diagnose and fix the root cause. Common cross-plan issues to check:
- `run_embed` signature change (verbose param) breaking existing tests that call `run_embed(root, cfg)` without verbose — should be fine since `verbose=False` is default
- Import errors in `carta/mcp/server.py` if mcp SDK not installed — run `pip install -e ".[dev]"` first
- `_install_skills` references in existing bootstrap tests — those tests may need updating since the function is removed

Also verify the MCP server can be imported and started (without blocking):
```bash
python3 -c "from carta.mcp.server import main, mcp_server; print(f'Server name: {mcp_server.name}')"
```

Verify .mcp.json is parseable:
```bash
python3 -c "import json; d=json.load(open('.mcp.json')); print(json.dumps(d, indent=2))"
```

Verify no plugin cache residue (check both paths):
```bash
python3 -c "
from pathlib import Path
p1 = Path.home() / '.claude/plugins/carta'
p2 = Path.home() / '.claude/plugins/cache/carta-cc'
print(f'plugins/carta exists: {p1.exists()}')
print(f'plugins/cache/carta-cc exists: {p2.exists()}')
"
```
  </action>
  <verify>
    <automated>cd /Users/ian/dev/doc-audit-cc && python3 -m pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest -x -q` exits 0 with all tests passing
    - `python3 -c "from carta.mcp.server import main"` exits 0
    - `.mcp.json` parses as valid JSON with mcpServers.carta.command == "carta-mcp"
  </acceptance_criteria>
  <done>Full test suite green. MCP server importable. .mcp.json valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify carta-mcp produces clean JSON-RPC stream</name>
  <files></files>
  <action>
Human verification of MCP server wire-protocol discipline. The executor presents the verification steps below and pauses for user confirmation.
  </action>
  <what-built>
    MCP stdio server scaffold (carta-mcp), pipeline reliability fixes (batch upsert, timeout, overlap cap, verbose suppression, sidecar heal), plugin cache cleanup, .mcp.json registration.
  </what-built>
  <how-to-verify>
    1. Run `carta-mcp` in a terminal (it will block waiting for JSON-RPC input on stdin)
    2. Verify that NO output appears on stdout before you send input (clean start)
    3. Press Ctrl+C to exit
    4. Run `carta-mcp 2>mcp-stderr.log` and check mcp-stderr.log for any log lines — they should be WARNING level or above, formatted as "WARNING [carta-mcp] ..."
    5. Verify `cat .mcp.json` shows the correct registration
    6. Verify `ls ~/.claude/plugins/carta/ 2>/dev/null` returns "No such file or directory"
    7. Verify `ls ~/.claude/plugins/cache/carta-cc/ 2>/dev/null` returns "No such file or directory"
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify — requires manual approval"</automated>
  </verify>
  <done>Human confirmed carta-mcp starts cleanly with no stdout pollution. No plugin cache residue.</done>
  <resume-signal>Type "approved" if carta-mcp starts cleanly with no stdout pollution, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
All verification is handled by Task 1 (automated) and Task 2 (human-verify).
</verification>

<success_criteria>
1. Full test suite passes (all plans combined)
2. carta-mcp starts with clean JSON-RPC stream on stdout
3. No plugin cache residue
4. Human approves the MCP server behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-reliability-mcp-foundation/01-03-SUMMARY.md`
</output>
