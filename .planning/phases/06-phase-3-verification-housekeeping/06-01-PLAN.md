---
phase: 06-phase-3-verification-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [EMBED-01]

must_haves:
  truths:
    - "Phase 3 directory contains a VERIFICATION.md with SATISFIED status for HOOK-01 through HOOK-07 and EMBED-01"
    - "VERIFICATION.md includes cross-reference note that hook wiring was completed in Phase 5"
    - "ROADMAP.md progress table shows Phase 5 as Complete (not 0/1 Pending)"
    - "REQUIREMENTS.md checkboxes for HOOK-01 through HOOK-07 and EMBED-01 are marked [x]"
  artifacts:
    - path: ".planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md"
      provides: "Phase 3 verification certificate covering all 8 requirements"
      contains: "EMBED-01"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate progress table for all completed phases"
      contains: "Complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkbox status for HOOK and EMBED requirements"
      contains: "[x] **HOOK-01**"
  key_links:
    - from: ".planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md"
      to: ".planning/phases/05-hook-wiring-entry-point-fix/05-VERIFICATION.md"
      via: "Cross-reference note in Phase 3 VERIFICATION.md"
      pattern: "Phase 5"
---

<objective>
Write the missing Phase 3 VERIFICATION.md certifying all HOOK-01 through HOOK-07 and EMBED-01 requirements, then update stale progress entries in ROADMAP.md, REQUIREMENTS.md, and STATE.md.

Purpose: Close the verification gap for Phase 3 and bring project tracking artifacts into accurate state.
Output: 03-VERIFICATION.md, updated ROADMAP.md progress table, updated REQUIREMENTS.md checkboxes, updated STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-phase-3-verification-housekeeping/06-CONTEXT.md
@.planning/phases/05-hook-wiring-entry-point-fix/05-VERIFICATION.md
@.planning/phases/03-smart-hook-markdown-embedding/03-01-PLAN.md
@.planning/phases/03-smart-hook-markdown-embedding/03-01-SUMMARY.md
@.planning/phases/03-smart-hook-markdown-embedding/03-02-SUMMARY.md
@.planning/phases/03-smart-hook-markdown-embedding/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 3 VERIFICATION.md</name>
  <files>.planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-hook-wiring-entry-point-fix/05-VERIFICATION.md (structural template — follow this format exactly)
    - .planning/phases/03-smart-hook-markdown-embedding/03-01-PLAN.md (must_haves: truths, artifacts, key_links to verify)
    - .planning/phases/03-smart-hook-markdown-embedding/03-01-SUMMARY.md (implementation evidence for plan 01)
    - .planning/phases/03-smart-hook-markdown-embedding/03-02-SUMMARY.md (implementation evidence for plan 02)
    - .planning/phases/03-smart-hook-markdown-embedding/03-03-SUMMARY.md (implementation evidence for plan 03)
    - .planning/REQUIREMENTS.md (exact requirement text for HOOK-01 through HOOK-07 and EMBED-01)
    - carta/config.py (verify DEFAULTS contains high_threshold, low_threshold, max_results, judge_timeout_s, ollama_model)
    - carta/embed/parse.py (verify extract_markdown_text exists)
    - carta/embed/pipeline.py (verify .md in _SUPPORTED_EXTENSIONS and markdown dispatch in _embed_one_file)
    - carta/embed/induct.py (verify file_type field in generate_sidecar_stub)
    - carta/hook/hook.py (verify score-band routing: high_threshold >0.85 inject, <0.60 discard, gray zone judge call, 3s timeout, max 5 chunks)
    - carta/hooks/carta-prompt-hook.sh (verify shell stub with exec carta-hook)
  </read_first>
  <action>
    Create `.planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md` following the exact structure of `05-VERIFICATION.md` as a template. The document must contain these sections in order:

    **Frontmatter:**
    ```yaml
    ---
    phase: 03-smart-hook-markdown-embedding
    verified: 2026-03-28T00:00:00Z
    status: passed
    score: "N/N must-haves verified"  # fill in actual count after verification
    re_verification: false
    ---
    ```

    **Title and header:**
    `# Phase 03: Smart Hook + Markdown Embedding -- Verification Report`
    Phase Goal from ROADMAP: "Relevant documentation surfaces automatically on UserPromptSubmit without context noise; markdown files are embeddable alongside PDFs"
    Status: PASSED

    **Observable Truths table (per D-02, verify all 8 requirements):**
    Build from the Phase 3 plan must_haves.truths plus the ROADMAP success criteria. Each truth gets a row with Status and Evidence columns. Use grep/file-read evidence from the actual source files listed in read_first above. For each truth:
    - Read the relevant source file
    - Grep for the specific function, constant, or pattern
    - Record file + line number as evidence

    Truths to verify (derived from ROADMAP Phase 3 success criteria):
    1. High-similarity prompt (score >0.85) triggers immediate injection without Ollama — evidence from hook.py score routing
    2. Low-similarity prompt (score <0.60) discards candidates — evidence from hook.py noise gate
    3. Gray-zone prompt (0.60-0.85) calls Ollama judge; 3s timeout fails open — evidence from hook.py _judge_with_timeout
    4. Maximum 5 chunks injected per prompt — evidence from hook.py max_results slicing
    5. Thresholds and judge model configurable in .carta/config.yaml — evidence from config.py DEFAULTS
    6. Markdown files embeddable via carta embed — evidence from pipeline.py _SUPPORTED_EXTENSIONS and dispatch
    7. Sidecar for .md files contains file_type: markdown — evidence from induct.py generate_sidecar_stub

    **Required Artifacts table:**
    List each artifact from the plan must_haves.artifacts plus hook module files, with VERIFIED status and grep evidence.

    **Key Link Verification table:**
    Verify each key_link from the plan must_haves plus hook wiring links.

    **Behavioral Spot-Checks table:**
    Run and record results of:
    - `python -m pytest carta/embed/tests/test_embed.py -v -q` (markdown embedding tests)
    - `python -m pytest carta/hook/tests/test_hook.py -v -q` (hook behavior tests)
    - `python -m pytest carta/tests/test_config.py -v -q` (config tests)

    **Requirements Coverage table (per D-02):**
    One row per requirement HOOK-01 through HOOK-07 and EMBED-01. Each row has: Requirement ID, Source Plan, Description (from REQUIREMENTS.md), Status = SATISFIED (per D-01 for EMBED-01), Evidence (file:line or grep match).

    **Cross-reference note (per D-03):**
    Add a clearly visible note after the Requirements Coverage section:
    ```
    > **Cross-phase note:** Hook behavioral implementation (score routing, Ollama judge, config schema)
    > was completed in Phase 3 (plans 03-01, 03-02, 03-03). Hook entry point registration, shell stub
    > wiring, and HOOK-05 fail-open timeout fix were completed in Phase 5 (plan 05-01).
    > See `05-VERIFICATION.md` for wiring-layer verification.
    ```

    **Gaps Summary:** "No gaps" if all requirements SATISFIED.

    IMPORTANT: Run the actual grep/test commands to gather real evidence. Do NOT fabricate line numbers or test counts. Every evidence cell must come from an actual file read or command output during this task.
  </action>
  <verify>
    <automated>grep -c "SATISFIED" .planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md exists
    - File contains `status: passed` in frontmatter
    - File contains `HOOK-01` through `HOOK-07` each followed by `SATISFIED` on the same table row
    - File contains `EMBED-01` followed by `SATISFIED` on the same table row
    - File contains the string "Phase 5" in a cross-reference note (per D-03)
    - File contains "05-VERIFICATION.md" (cross-reference link per D-03)
    - File contains at least one `python -m pytest` spot-check result
    - grep -c "SATISFIED" returns 8 (one per requirement)
  </acceptance_criteria>
  <done>Phase 3 VERIFICATION.md exists with SATISFIED status for all 8 requirements (HOOK-01 through HOOK-07 + EMBED-01), cross-reference to Phase 5 wiring verification, and real test/grep evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update stale ROADMAP.md, REQUIREMENTS.md, and STATE.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — editing progress table and Phase 6 plan list)
    - .planning/REQUIREMENTS.md (full file — updating checkboxes)
    - .planning/STATE.md (full file — updating current position and todos)
  </read_first>
  <action>
    **ROADMAP.md progress table fixes:**
    In the `## Progress` table at the bottom, update these rows:
    - Phase 5 row: change `0/1 | Pending | -` to `1/1 | Complete | 2026-03-27`
    - Phase 6 row: change `0/1 | Pending | -` to `0/1 | In Progress | -` (this plan is executing)

    Also update Phase 6 Plans line from `**Plans:** 0 plans` to `**Plans:** 1 plan`

    **REQUIREMENTS.md checkbox updates:**
    Change these lines from `- [ ]` to `- [x]`:
    - `- [ ] **HOOK-01**:` to `- [x] **HOOK-01**:`
    - `- [ ] **HOOK-02**:` to `- [x] **HOOK-02**:`
    - `- [ ] **HOOK-03**:` to `- [x] **HOOK-03**:`
    - `- [ ] **HOOK-04**:` to `- [x] **HOOK-04**:`
    - `- [ ] **HOOK-05**:` to `- [x] **HOOK-05**:`
    - `- [ ] **HOOK-06**:` to `- [x] **HOOK-06**:`
    - `- [ ] **HOOK-07**:` to `- [x] **HOOK-07**:`
    - `- [ ] **EMBED-01**:` to `- [x] **EMBED-01**:`

    In the Traceability table, update these rows:
    - HOOK-01 through HOOK-07: change `Pending` to `Complete`
    - EMBED-01: change `Pending` to `Complete`

    Update the Coverage section:
    - Change `Pending (gap closure): 8 (HOOK-01-07, EMBED-01)` to `Pending (gap closure): 0`

    **STATE.md updates:**
    - Update `status:` frontmatter to `In Progress`
    - Update `progress.completed_phases:` to `6` (phases 01-05 done, 06 executing)
    - Update `Current focus:` to `Phase 06 (executing)`
    - Update `Phase:` under Current Position to `06`
    - Update `Plan:` under Current Position to `01 (in progress)`
    - Mark these todos as done with [x]:
      - `[ ] Begin Phase 1: batch Qdrant upsert in pipeline.py` to `[x]`
      - `[ ] Phase 1: per-file timeout in pipeline.py` to `[x]`
      - `[ ] Phase 1: verbose=False parameter on all pipeline service functions` to `[x]`
  </action>
  <verify>
    <automated>grep "HOOK-01" .planning/REQUIREMENTS.md | head -1 | grep -c "\[x\]"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md progress table Phase 5 row contains `1/1 | Complete | 2026-03-27`
    - ROADMAP.md progress table Phase 6 row contains `In Progress`
    - REQUIREMENTS.md contains `- [x] **HOOK-01**:`
    - REQUIREMENTS.md contains `- [x] **HOOK-07**:`
    - REQUIREMENTS.md contains `- [x] **EMBED-01**:`
    - REQUIREMENTS.md Traceability table contains `EMBED-01 | Phase 6 | Complete`
    - REQUIREMENTS.md contains `Pending (gap closure): 0`
    - STATE.md contains `status: In Progress`
    - STATE.md contains `completed_phases: 6`
    - STATE.md todos for Phase 1 batch upsert, per-file timeout, and verbose=False are marked `[x]`
  </acceptance_criteria>
  <done>ROADMAP progress table accurate for all phases. REQUIREMENTS.md shows all 23 v1 requirements as [x] complete. STATE.md reflects current position.</done>
</task>

</tasks>

<verification>
- `grep -c "SATISFIED" .planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md` returns 8
- `grep "Phase 5" .planning/phases/03-smart-hook-markdown-embedding/03-VERIFICATION.md` returns cross-reference note
- `grep "Complete" .planning/ROADMAP.md | grep "Phase 5"` shows updated progress
- `grep -c "\[x\]" .planning/REQUIREMENTS.md` shows all v1 requirements checked
</verification>

<success_criteria>
1. Phase 3 VERIFICATION.md exists with SATISFIED for all 8 requirements (HOOK-01-07 + EMBED-01)
2. VERIFICATION.md cross-references Phase 5 for wiring verification (per D-03)
3. ROADMAP.md progress table is accurate for all 6 phases
4. REQUIREMENTS.md checkboxes updated for HOOK-01-07 and EMBED-01
5. STATE.md reflects current project position
</success_criteria>

<output>
After completion, create `.planning/phases/06-phase-3-verification-housekeeping/06-01-SUMMARY.md`
</output>
