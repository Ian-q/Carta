---
phase: 999.2
slug: vision-model-pipeline-for-image-embedded-pdf-content
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-29
---

# Phase 999.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.0+ |
| **Config file** | `pyproject.toml` (pytest config section) |
| **Quick run command** | `python3 -m pytest carta/tests/test_vision.py -x -q` |
| **Full suite command** | `python3 -m pytest carta/tests/ -q` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `python3 -m pytest carta/tests/test_vision.py -x -q`
- **After every plan wave:** Run `python3 -m pytest carta/tests/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01-01 | 01 | 1 | VIS-01..VIS-06 | unit (RED) | `pytest carta/tests/test_vision.py --collect-only` | Wave 0 | pending |
| 01-02 | 01 | 1 | VIS-01..VIS-06 | unit (GREEN) | `pytest carta/tests/test_vision.py -x -q` | Wave 0 | pending |
| 02-01 | 02 | 2 | VIS-07..VIS-09 | integration | `python3 -c "from carta.embed.induct import generate_sidecar_stub; ..."` | existing | pending |
| 02-02 | 02 | 2 | VIS-07..VIS-09 | unit | `pytest carta/tests/test_pipeline.py -x -q` | existing (extend) | pending |

*Status: pending -- all tests to be created in Wave 0 of Plan 01*

---

## Wave 0 Requirements

- [ ] `carta/tests/test_vision.py` — 9+ test stubs for VIS-01 through VIS-06 (created in Plan 01, Task 1)
- [ ] Mock fixtures for fitz.Pixmap, fitz.open, requests.post (created in Plan 01, Task 1)
- [ ] No new framework install needed — pytest already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| LLaVA produces useful descriptions of real datasheets | VIS-02 (accuracy) | Requires Ollama + LLaVA running with real PDF | Run `carta embed` on a datasheet PDF, then `carta search "temperature curve"` — verify image description chunks appear in results |

*All other behaviors have automated verification via mocked unit tests.*

---

## Requirement Definitions (Phase 999.2)

| Req ID | Description | Source Decision |
|--------|-------------|-----------------|
| VIS-01 | `extract_image_descriptions()` extracts embedded images from PDF pages via PyMuPDF | D-04, D-05 |
| VIS-02 | CMYK images converted to RGB before PNG encoding | D-06 |
| VIS-03 | Ollama vision model called with base64 PNG and technical extraction prompt | D-07 |
| VIS-04 | Each image description becomes a separate chunk with `doc_type: "image_description"` | D-08, D-09 |
| VIS-05 | Vision model errors are fail-open — skip image, log warning, continue | D-11, D-12 |
| VIS-06 | Config key `ollama_vision_model` defaults to `llava:latest` | D-01, D-02, D-03 |
| VIS-07 | `_embed_one_file()` calls vision module for .pdf files, merges image chunks | D-10 |
| VIS-08 | Sidecar schema includes `image_count` and `image_chunks` fields | D-13, D-14 |
| VIS-09 | Image chunk_index values start after text chunks to avoid point ID collisions | Research Pitfall 5 |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
